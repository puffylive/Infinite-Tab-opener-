import webbrowser, time
url = input("Enter url: ")
duration = input("Enter How long you want to wait untill another tab opens: ")
while True:
    webbrowser.open_new(url)
    time.sleep(int(duration))
